# KTV API — Python deps
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
yt-dlp>=2024.10.7
imageio-ffmpeg>=0.5.1
# Optional (heavy): demucs needs torch CUDA. Install separately via:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install demucs
# On Python 3.14 these are not yet available; system gracefully skips vocal removal.

# Test deps
pytest>=8.3
pytest-asyncio>=0.24
